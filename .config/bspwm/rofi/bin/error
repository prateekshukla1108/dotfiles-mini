#!/usr/bin/env bash

# rofi command #
ROFI_COMMAND="rofi -theme ~/.config/bspwm/rofi/themes/error.rasi"

# Prompt message #
PROMPT="[Error] Theme not found ✘"

# Displaying the error message #
${ROFI_COMMAND} -e "${PROMPT}"
